% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-delim.R
\name{btwrite}
\alias{btwrite}
\title{Write a data frame to a delimited text file}
\usage{
btwrite(
  x,
  file,
  delim = ",",
  na = "NA",
  col_names = TRUE,
  quote = "\\"",
  digits = 15,
  append = FALSE,
  n_threads = bt_default_threads()
)
}
\arguments{
\item{x}{A data.frame.}

\item{file}{Output path.}

\item{delim}{Field delimiter.}

\item{na}{String to write for \code{NA}.}

\item{col_names}{Logical; write a header row?}

\item{quote}{Quoting character; a field is quoted only if it contains the
delimiter, the quote character, or a newline.}

\item{digits}{Significant digits for double columns (passed to \verb{\%g}).}

\item{append}{Logical; append to \code{file} instead of overwriting (no header is
written when appending).}

\item{n_threads}{Number of worker threads.}
}
\value{
\code{file}, invisibly.
}
\description{
A threaded C++ writer: each column is resolved to a plain C array on the R
thread, then disjoint row ranges are formatted into private buffers and
flushed in order.
}
\examples{
p <- tempfile(fileext = ".csv")
btwrite(head(iris), p)
btread(p)
}
