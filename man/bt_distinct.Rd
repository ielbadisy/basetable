% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-file.R
\name{bt_distinct}
\alias{bt_distinct}
\title{Distinct key combinations straight off a delimited file}
\usage{
bt_distinct(
  file,
  cols,
  where = NULL,
  delim = NULL,
  quote = "\\"",
  comment = "",
  header = TRUE,
  skip = 0,
  n_max = Inf,
  na = c("NA", ""),
  n_threads = bt_default_threads(),
  as = c("data.frame", "data.table")
)
}
\arguments{
\item{file}{Path to a delimited text file (\code{.gz} supported).}

\item{cols}{Columns whose distinct combinations to return.}

\item{where}{Optional character vector of simple row filters applied \emph{in the
same pass} (predicate pushdown), each \code{"<col> <op> <literal>"} with \code{op}
one of \verb{< <= > >= == !=}. String literals may be quoted. All are AND-ed.}

\item{delim, quote, comment, header, skip, n_max, na}{See \code{\link[=btread]{btread()}}.}

\item{n_threads}{Worker threads.}

\item{as}{Return \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
A data.frame (or data.table) of the distinct combinations.
}
\description{
One-pass unique combinations of \code{cols} (like \code{unique(x, by = cols)} but
keeping only \code{cols}).
}
