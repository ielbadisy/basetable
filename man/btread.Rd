% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-delim.R
\name{btread}
\alias{btread}
\title{Read a delimited text file}
\usage{
btread(
  file,
  delim = NULL,
  header = TRUE,
  col_names = NULL,
  col_types = NULL,
  col_select = NULL,
  na = c("NA", ""),
  quote = "\\"",
  comment = "",
  trim_ws = FALSE,
  skip = 0,
  n_max = Inf,
  guess_max = 10000,
  lazy = FALSE,
  n_threads = bt_default_threads(),
  as = c("data.frame", "basetable")
)
}
\arguments{
\item{file}{Path to a delimited text file. \code{.gz} inputs are decompressed to
a temporary file first.}

\item{delim}{Field delimiter. \code{NULL} (default) sniffs the first line for
one of \verb{,} \verb{\\t} \verb{;} \code{|} and a space.}

\item{header}{Logical; does the first row hold column names?}

\item{col_names}{Optional character vector of names, used only when
\code{header = FALSE}. Defaults to \code{V1}, \code{V2}, ...}

\item{col_types}{\code{NULL} to guess, or a character vector of
\code{"logical"}, \code{"integer"}, \code{"double"}, \code{"character"}, \code{"skip"}, \code{"guess"}.
Length 1 is recycled; otherwise it must have one entry per column.}

\item{col_select}{Optional integer positions or character names of the
columns to keep. Other columns are not parsed.}

\item{na}{Character vector of strings to read as \code{NA}.}

\item{quote}{Quoting character.}

\item{comment}{Lines beginning with this one-character string are skipped
(\code{""} disables).}

\item{trim_ws}{Logical; strip leading/trailing spaces and tabs from unquoted
fields.}

\item{skip}{Number of raw lines to discard before reading.}

\item{n_max}{Maximum number of data rows to read (\code{Inf} for all).}

\item{guess_max}{Number of rows sampled for type guessing.}

\item{lazy}{Logical; return numeric columns as lazily-parsed ALTREP vectors.}

\item{n_threads}{Number of worker threads for the eager numeric fill.}

\item{as}{Return \code{"data.frame"} (default) or \code{"basetable"}.}
}
\value{
A data.frame.
}
\description{
A from-scratch delimited-file reader written in C++: the file is memory
mapped, scanned once for row boundaries (RFC 4180 quoting), type-guessed
from a bounded sample, and materialised into typed vectors. Numeric columns
are filled by a thread pool; character columns are built on the R thread.
}
\details{
With \code{lazy = TRUE} the integer and double columns are returned as ALTREP
vectors that parse their column only the first time it is touched, which
makes "read a few columns from a wide file" close to free.
}
\examples{
p <- tempfile(fileext = ".csv")
write.csv(head(iris), p, row.names = FALSE)
btread(p)
}
