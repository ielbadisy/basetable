% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nest.R
\name{unnest}
\alias{unnest}
\title{Expand list-columns back into rows}
\usage{
unnest(data, cols)
}
\arguments{
\item{data}{A data frame with one or more list-columns.}

\item{cols}{Character vector of list-columns to expand. When several are
given, their elements must have matching lengths within each row.}
}
\value{
A \code{basetable} with one row per element.
}
\description{
\code{unnest()} is the inverse of \code{\link[=nest]{nest()}}. Each element of a list-column is
expanded into rows, and the other columns are repeated to match. Elements
may be data frames (their columns are added) or atomic vectors (the values
are stored under the list-column's own name). Rows whose element is empty
are dropped.
}
\examples{
nested <- nest(data.frame(g = c("a", "a", "b"), x = 1:3), by = "g")
unnest(nested, "data")
}
\seealso{
\code{\link[=nest]{nest()}}
}
