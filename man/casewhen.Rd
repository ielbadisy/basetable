% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{casewhen}
\alias{casewhen}
\title{Multi-branch selection by condition}
\usage{
casewhen(conditions, default = NA)
}
\arguments{
\item{conditions}{A named list of equal-length logical vectors. Each name
is the value returned where its vector is the first \code{TRUE}.}

\item{default}{Value for positions where no condition holds. Default \code{NA}.}
}
\value{
A vector as long as the conditions, holding matched labels and
\code{default} elsewhere.
}
\description{
For each position, take the label of the first condition that is \code{TRUE}
there. Conditions are a named list of equal-length logical vectors, in the
same shape as \code{\link{collapsevalues}}'s \code{groups}: the name of an
element is the value used where that element is the first \code{TRUE}.
Positions matching no condition get \code{default}. \code{NA} in a condition
counts as not matching.
}
\examples{
x <- c(-3, 0, 4, 25)
casewhen(list(neg = x < 0, low = x < 10), default = "high")
}
