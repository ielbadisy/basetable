% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nest.R
\name{nest}
\alias{nest}
\title{Nest rows into a list-column of tables}
\usage{
nest(data, by, name = "data")
}
\arguments{
\item{data}{A data frame.}

\item{by}{Character vector of grouping columns.}

\item{name}{Name of the new list-column.}
}
\value{
A \code{basetable} with the \code{by} columns and one list-column holding the
remaining columns of each group as a data frame.
}
\description{
\code{nest()} collapses the rows of each group into a data frame and stores those
data frames in a single list-column, giving one row per group. Groups appear
in order of first occurrence. Missing values in \code{by} form their own group.
}
\examples{
nested <- nest(data.frame(g = c("a", "a", "b"), x = 1:3), by = "g")
nested
nested$data[[1]]
}
\seealso{
\code{\link[=unnest]{unnest()}}, \code{\link[=split]{split()}}
}
