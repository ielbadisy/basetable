% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-file.R
\name{bt_aggregate}
\alias{bt_aggregate}
\title{Grouped aggregation straight off a delimited file}
\usage{
bt_aggregate(
  file,
  by,
  value = NULL,
  fun = "sum",
  where = NULL,
  na.rm = TRUE,
  delim = NULL,
  quote = "\\"",
  comment = "",
  header = TRUE,
  skip = 0,
  n_max = Inf,
  na = c("NA", ""),
  n_threads = bt_default_threads(),
  as = c("data.frame", "basetable")
)
}
\arguments{
\item{file}{Path to a delimited text file (\code{.gz} supported).}

\item{by}{Character vector of grouping column names (or 1-based positions).}

\item{value}{Character vector of numeric column names to aggregate (or
positions). Defaults to every column not in \code{by}.}

\item{fun}{One or more of \code{"sum"}, \code{"mean"}, \code{"var"}, \code{"sd"}, \code{"min"},
\code{"max"}, \code{"n"}. With several, output columns are named \verb{<value>_<fun>}.}

\item{where}{Optional character vector of simple row filters applied \emph{in the
same pass} (predicate pushdown), each \code{"<col> <op> <literal>"} with \code{op}
one of \verb{< <= > >= == !=}. String literals may be quoted. All are AND-ed.}

\item{na.rm}{Logical; drop \code{NA} values before reducing.}

\item{delim, quote, comment, header, skip, n_max, na}{See \code{\link[=btread]{btread()}}.}

\item{n_threads}{Worker threads.}

\item{as}{Return \code{"data.frame"} (default) or \code{"basetable"}.}
}
\value{
A data.frame: the \code{by} columns, then one column per
\code{value} x \code{fun}.
}
\description{
A fused one-pass group-by: \code{\link[=btread]{btread()}}'s reader machinery
(memory map, parallel row index) is used to scan the file once, extracting
only the grouping and value fields, grouping, and reducing. The grouping and
value columns are never materialised as R vectors, so on a wide file this is
typically 2-3x faster end-to-end than reading the whole file and grouping it.
}
\examples{
p <- tempfile(fileext = ".csv")
write.csv(data.frame(g = rep(letters[1:3], 4), x = 1:12), p, row.names = FALSE)
bt_aggregate(p, by = "g", value = "x", fun = c("sum", "mean"))
}
